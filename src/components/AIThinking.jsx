import { ArrowDown, ArrowRight, BrainCircuit, Goal, ShieldAlert, Sparkles, TrendingUp, WalletCards, X } from 'lucide-react'
const roles = [{label:'Cashflow',icon:WalletCards,input:'Balance + commitments',output:'Available financial buffer'},{label:'Behaviour',icon:TrendingUp,input:'Synthetic transactions',output:'Spending pressure signals'},{label:'Scenario',icon:Sparkles,input:'Decision + alternatives',output:'Possible futures'},{label:'Risk',icon:ShieldAlert,input:'Buffer + stress cases',output:'Resilience rating'},{label:'Goal',icon:Goal,input:'Targets + savings',output:'Goal collision'}]
export function AIThinking({ open, setOpen }) { return <><section className="ms-thinking"><div><p className="ms-kicker"><i/> HOW MONEY SANDBOX THINKS</p><h2>Five teammates.<br/>One clear decision.</h2><p>Specialised AI teammates interpret the context. Deterministic code handles every balance, projection, and risk number.</p><button onClick={() => setOpen(true)}>View details <ArrowRight size={15}/></button></div><div className="ms-agent-minis">{roles.map(({label,icon:Icon}) => <div key={label}><span><Icon size={16}/></span><b>{label}</b></div>)}<div className="judge"><BrainCircuit size={16}/><b>Decision Judge</b></div></div></section>{open && <div className="ms-modal-backdrop" onClick={() => setOpen(false)}><aside className="ms-drawer" onClick={event => event.stopPropagation()}><button onClick={() => setOpen(false)} aria-label="Close AI details"><X size={19}/></button><p className="ms-kicker"><i/> AUTONOMOUS AI TEAMMATES</p><h2>One decision,<br/>five perspectives.</h2><div className="ms-architecture"><b>USER</b><ArrowDown size={14}/><b>ORCHESTRATOR</b><ArrowDown size={14}/><span>Specialised teammates</span></div><p>AI handles reasoning, scenario interpretation, and explanations. The deterministic simulation engine handles arithmetic, projections, risk formulas and numerical outputs.</p><div>{roles.map(({label,icon:Icon,input,output}) => <article key={label}><span><Icon size={18}/></span><div><b>{label} teammate <em>Ready</em></b><p><strong>Input:</strong> {input}<br/><strong>Output:</strong> {output}</p></div></article>)}</div><footer><BrainCircuit size={18}/><span><small>DETERMINISTIC ENGINE → DECISION JUDGE</small><b>Combines verified evidence into an explainable verdict.</b></span></footer></aside></div>}</> }
