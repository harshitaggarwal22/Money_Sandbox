import { ArrowRight, CalendarDays } from 'lucide-react'

export function DecisionInput({ decision, setDecision, onSimulate, error, isLoading }) {
  const update = (key, value) => setDecision(current => ({ ...current, [key]: value }))
  return <section className="ms-decision-card"><div><p className="ms-kicker"><i/> WHAT ARE YOU PLANNING?</p><h2>Test a purchase before you make it.</h2></div><div className="ms-fields"><label>Decision name<input value={decision.name} onChange={event => update('name', event.target.value)} /></label><label>Amount<div className="ms-money"><span>₹</span><input aria-label="Purchase amount" type="number" min="0" value={decision.amount} onChange={event => update('amount', event.target.value)} /></div></label><label>Category<input value={decision.category} onChange={event => update('category', event.target.value)} /></label><label>Purchase date<div className="ms-date"><CalendarDays size={14}/><select value={decision.purchaseDay} onChange={event => update('purchaseDay', Number(event.target.value))}><option value={0}>Today</option><option value={7}>In 7 days</option><option value={12}>In 12 days</option></select></div></label></div>{error && <p className="ms-form-error">{error}</p>}<button className="ms-primary" onClick={onSimulate} disabled={isLoading}>{isLoading ? 'Simulating…' : <>Simulate this decision <ArrowRight size={17}/></>}</button><p className="ms-disclaimer">Uses synthetic demo data. No real bank account is connected.</p></section>
}
