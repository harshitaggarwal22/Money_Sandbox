export function DevNetworkInfo() { if (!import.meta.env.DEV) return null; const port = window.location.port || '5173'; return <aside className="ms-network"><b>DEVICE PREVIEW</b><span>● Server reachable</span><code>Local: http://localhost:{port}</code><code>Network: http://&lt;LOCAL-IP&gt;:{port}</code><small>Find your laptop IPv4 address with <code>ipconfig</code>, then open the Network URL on your phone.</small></aside> }
