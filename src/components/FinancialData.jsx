import { Check, Database, LockKeyhole, RefreshCw } from 'lucide-react'

export function FinancialData({ connected, onConnect, loading }) {
  return <section className={`ms-data-card ${connected ? 'connected' : ''}`}><div><p className="ms-kicker"><i/> FINANCIAL DATA</p><h2>Demo financial picture</h2><p>{connected ? 'Your synthetic financial profile is ready for simulation.' : 'Load a synthetic profile to make the demo feel like a connected financial workspace.'}</p><span className="ms-consent"><LockKeyhole size={12}/> Consent-based data access</span></div><div className="ms-data-status">{['Demo Account Connected', 'Transaction History Imported', 'Spending Behaviour Analysed', 'Goals Loaded'].map((label, index) => <span key={label} className={connected ? 'done' : index === 0 ? 'ready' : ''}><i>{connected ? <Check size={11}/> : index === 0 ? <Database size={11}/> : null}</i>{label}</span>)}<button className="ms-outline" onClick={onConnect} disabled={loading}>{loading ? <><RefreshCw className="spin" size={14}/>Importing…</> : connected ? 'Refresh demo data' : 'Connect Demo Account'}</button></div><small>Future integration could connect consented financial data sources to reduce manual entry. This prototype never connects to real banks.</small></section>
}
