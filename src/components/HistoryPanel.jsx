import { RotateCcw, Trash2 } from 'lucide-react'
import { formatINR } from '../utils/currency'
import { RiskBadge } from './ScenarioSelector'

export function HistoryPanel({ history, onRestore, onClear }) { return <section className="ms-page-card ms-history"><div className="ms-page-heading"><div><p className="ms-kicker"><i/> SIMULATION HISTORY</p><h1>Your previous decisions</h1><p>Every simulation is saved only in this browser.</p></div>{history.length > 0 && <button className="ms-outline danger-button" onClick={onClear}><Trash2 size={15}/>Clear history</button>}</div>{history.length === 0 ? <div className="ms-empty-state"><RotateCcw size={22}/><b>No simulations yet</b><span>Run a decision simulation and it will appear here.</span></div> : <div className="ms-history-list">{history.map(item => <button key={item.id} onClick={() => onRestore(item)}><span><b>{item.decision.name}</b><small>{new Date(item.createdAt).toLocaleDateString('en-IN', { day:'numeric', month:'short' })}</small></span><span>{formatINR(item.decision.amount)}</span><span>{item.scenario.label}</span><RiskBadge risk={item.result.risk}/><strong>{formatINR(item.result.buffer)}</strong></button>)}</div>}</section> }
