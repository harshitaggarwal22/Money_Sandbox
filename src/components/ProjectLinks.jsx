import { ExternalLink, Link2, QrCode } from 'lucide-react'
import { projectLinks } from '../data/demoData'

export function ProjectLinks() { const configured = projectLinks.liveDemo && !projectLinks.liveDemo.includes('your-live-demo-url'); return <section className="ms-project-links"><div><p className="ms-kicker"><i/> PROJECT LINKS</p><h1>Share the finished story.</h1><p>Update these placeholders in <code>src/data/demoData.js</code> before submission.</p><div className="ms-link-list">{[['Live Demo',projectLinks.liveDemo],['GitHub Repository',projectLinks.repository],['Demo Video',projectLinks.demoVideo],['Presentation',projectLinks.presentation]].map(([name,url]) => <a key={name} href={url} target="_blank" rel="noreferrer"><Link2 size={14}/><span><b>{name}</b><small>{url}</small></span><ExternalLink size={13}/></a>)}</div></div><div className="ms-qr"><QrCode size={42}/>{configured ? <><div className="ms-qr-grid" aria-label="QR code for configured live demo"/><span>Scan to open live demo</span></> : <><b>Live demo URL not configured.</b><span>Add a Live Demo URL to generate a shareable QR block.</span></>}</div></section> }
